export class User {
    userId: string;
    userPassword: string;
    constructor(userId: string, userPassword: string) {
        this.userId = userId;
        this.userPassword = userPassword;
    }
}
